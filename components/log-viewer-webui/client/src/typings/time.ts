/**
 * Invalid ID for setInterval return value storage.
 * When clearInterval is called with this ID, it will not cause any effect.
 */
const SET_INTERVAL_INVALID_ID = 0;


export {SET_INTERVAL_INVALID_ID};
