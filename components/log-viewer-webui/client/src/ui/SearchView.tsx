import React from "react";

const SearchView = () => {
    return (
        <div>
            <h1>Search View</h1>
            <p>This is the Search view.</p>
        </div>
    );
};

export default SearchView;
