{
  "scripts": {
    "lint:check": "npx eslint --no-eslintrc --config package.json ../client ../imports ../server ../tests ../launcher.js",
    "lint:fix": "npm run lint:check -- --fix"
  },
  "devDependencies": {
    "eslint-config-yscope": "0.0.32"
  },
  "eslintConfig": {
    "extends": [
      "yscope/meteor"
    ]
  }
}
