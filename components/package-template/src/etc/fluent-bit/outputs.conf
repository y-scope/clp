# Output configuration for CLP operational logging
# Hot tier: Write to organized file storage for real-time access

# First-party services
[OUTPUT]
    Name file
    Match clp.compression-scheduler
    Path /var/log/clp/compression_scheduler
    Mkdir On

[OUTPUT]
    Name file
    Match clp.compression-worker
    Path /var/log/clp/compression_worker
    Mkdir On

[OUTPUT]
    Name file
    Match clp.spider-compression-worker
    Path /var/log/clp/spider_compression_worker
    Mkdir On

[OUTPUT]
    Name file
    Match clp.query-scheduler
    Path /var/log/clp/query_scheduler
    Mkdir On

[OUTPUT]
    Name file
    Match clp.query-worker
    Path /var/log/clp/query_worker
    Mkdir On

[OUTPUT]
    Name file
    Match clp.reducer
    Path /var/log/clp/reducer
    Mkdir On

[OUTPUT]
    Name file
    Match clp.garbage-collector
    Path /var/log/clp/garbage_collector
    Mkdir On

[OUTPUT]
    Name file
    Match clp.webui
    Path /var/log/clp/webui
    Mkdir On

[OUTPUT]
    Name file
    Match clp.api-server
    Path /var/log/clp/api_server
    Mkdir On

[OUTPUT]
    Name file
    Match clp.log-ingestor
    Path /var/log/clp/log_ingestor
    Mkdir On

[OUTPUT]
    Name file
    Match clp.mcp-server
    Path /var/log/clp/mcp_server
    Mkdir On
