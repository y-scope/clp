{{- include "clp.createLocalPv" (dict
  "root" .
  "name" (printf "%s-compression-worker-logs-pv" (include "clp.fullname" .))
  "component" "compression-worker"
  "capacity" "5Gi"
  "accessModes" (list "ReadWriteOnce")
  "hostPath" (printf "%s/compression-worker" .Values.clpConfig.logs_directory)
) }}
