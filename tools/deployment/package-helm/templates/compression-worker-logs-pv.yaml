{{- include "clp.createStaticPv" (dict
  "root" .
  "component_category" "compression-worker"
  "name" "logs"
  "capacity" "5Gi"
  "accessModes" (list "ReadWriteOnce")
  "hostPath" (printf "%s/compression_worker" .Values.clpConfig.logs_directory)
) }}
