{{- if .Values.clpConfig.reducer }}
{{- include "clp.createStaticPv" (dict
  "root" .
  "component_category" "reducer"
  "name" "logs"
  "capacity" "5Gi"
  "accessModes" (list "ReadWriteOnce")
  "hostPath" (printf "%s/reducer" .Values.clpConfig.logs_directory)
) }}
{{- end }}
