apiVersion: "v1"
kind: "PersistentVolumeClaim"
metadata:
  name: {{ include "clp.fullname" . }}-staged-archives
  labels:
    {{- include "clp.labels" . | nindent 4 }}
    app.kubernetes.io/component: "staged-archives"
spec:
  accessModes: ["ReadWriteMany"]
  storageClassName: "local-storage"
  selector:
    matchLabels:
      {{- include "clp.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: "staged-archives"
  resources:
    requests:
      storage: "20Gi"
