{{- include "clp.createLocalPv" (dict
  "root" .
  "name" (printf "%s-staged-streams" (include "clp.fullname" .))
  "component" "staged-streams"
  "capacity" "20Gi"
  "accessModes" (list "ReadWriteMany")
  "hostPath" "/tmp/clp/var/data/staged-streams"
) }}
