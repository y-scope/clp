{{- if .Values.clpConfig.mcp_server }}
{{- include "clp.createLocalPv" (dict
  "root" .
  "component" "mcp-server"
  "name" "logs"
  "nodeRole" "control-plane"
  "capacity" "5Gi"
  "accessModes" (list "ReadWriteOnce")
  "hostPath" (printf "%s/mcp-server" .Values.clpConfig.logs_directory)
) }}
{{- end }}
