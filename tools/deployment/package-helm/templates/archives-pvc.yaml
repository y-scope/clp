apiVersion: "v1"
kind: "PersistentVolumeClaim"
metadata:
  name: {{ include "clp.fullname" . }}-archives
  labels:
    {{- include "clp.labels" . | nindent 4 }}
    app.kubernetes.io/component: "archives"
spec:
  accessModes: ["ReadWriteMany"]
  storageClassName: "local-storage"
  selector:
    matchLabels:
      {{- include "clp.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: "archives"
  resources:
    requests:
      storage: "50Gi"
