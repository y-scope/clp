{{- include "clp.createLocalPv" (dict
  "root" .
  "component_category" "shared-data"
  "name" "streams"
  "nodeRole" "control-plane"
  "capacity" "20Gi"
  "accessModes" (list "ReadWriteMany")
  "hostPath" (printf "%s/streams" .Values.clpConfig.data_directory)
) }}
