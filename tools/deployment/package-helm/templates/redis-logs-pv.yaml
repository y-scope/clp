{{- include "clp.createLocalPv" (dict
  "root" .
  "name" (printf "%s-redis-logs" (include "clp.fullname" .))
  "component" "redis"
  "nodeRole" "control-plane"
  "capacity" "5Gi"
  "accessModes" (list "ReadWriteOnce")
  "hostPath" (printf "%s/redis" .Values.clpConfig.logs_directory)
) }}
