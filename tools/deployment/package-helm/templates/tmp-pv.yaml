{{- include "clp.createLocalPv" (dict
  "root" .
  "name" (printf "%s-tmp" (include "clp.fullname" .))
  "component" "tmp"
  "nodeRole" "control-plane"
  "capacity" "10Gi"
  "accessModes" (list "ReadWriteMany")
  "hostPath" .Values.clpConfig.tmp_directory
) }}
