{{- if or .Values.clpConfig.archive_output.retention_period
          .Values.clpConfig.results_cache.retention_period }}
{{- include "clp.createPvc" (dict
  "root" .
  "component_category" "garbage-collector"
  "name" "logs"
  "capacity" "5Gi"
  "accessModes" (list "ReadWriteOnce")
) }}
{{- end }}
