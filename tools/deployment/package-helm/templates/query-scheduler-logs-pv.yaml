{{- include "clp.createLocalPv" (dict
  "root" .
  "name" (printf "%s-query-scheduler-logs" (include "clp.fullname" .))
  "component" "query-scheduler-logs"
  "nodeRole" "control-plane"
  "capacity" "5Gi"
  "accessModes" (list "ReadWriteOnce")
  "hostPath" (printf "%s/query-scheduler" .Values.clpConfig.logs_directory)
) }}
