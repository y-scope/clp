version: "3"

tasks:
  package:
    vars:
      SRC_DIR: "{{.ROOT_DIR}}/tools/docker-images/clp-package"
    dir: "{{.SRC_DIR}}"
    sources:
      - "{{.SRC_DIR}}/**/*"
    deps:
      - ":package"
    cmds:
      - "./build.sh"
