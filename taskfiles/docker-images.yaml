version: "3"

tasks:
  package:
    dir: "{{.ROOT_DIR}}/tools/docker-images/clp-package"
    deps:
      - ":init"
      - ":core"
      - ":deps:spider"
      - ":python-libs"
      - ":webui"
    cmds:
      - "./build.sh"
