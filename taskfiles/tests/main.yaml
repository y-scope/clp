version: "3"

includes:
  integration: "integration.yaml"
  toolchains: "../toolchains.yaml"

tasks:
  rust-all:
    deps: ["toolchains:rust"]
    dir: "{{.ROOT_DIR}}"
    cmd: |-
      . {{.G_RUST_ENV_FILE}}
      cargo nextest run --all --all-features --release
